//This will generate HTML
const generateHTML = function (manager) {
	`<div> 
${manager.name} is my managers name
   
</div>`;
};

//use fs write file to make an HTML

module.exports = generateHTML();
